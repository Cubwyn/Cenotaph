# Documentation Archive

These files describe earlier project layouts and may contain obsolete paths.
Use the root `README.md`, `docs/README.md`, and current category documents for
active contracts. Archive files are retained only to preserve prior reasoning.
