================================================================================
CENOTAPH: THE GREAT OMISSION — PRIORITIZED TODO LIST
================================================================================
Generated: 2026-05-31
Files analyzed: 27 source files across src/
Total issues: 22 (4 critical, 10 design, 8 minor)
Dead code: ~594 lines (~22% of codebase)

================================================================================
PRIORITY 1 🔴 MUST FIX — Bugs that crash or corrupt
================================================================================

[ ] P1. Fix compile error: BroadPhaseBvh.as_query_pipeline() doesn't exist
    File: src/systems/physics/engine.rs:238
    Fix: Replace inline QueryPipeline construction. Rapier3D's
         BroadPhaseBvh has no as_query_pipeline() method. Use
         QueryPipeline::new() instead and update it each frame.
    Effort: Medium

[ ] P2. Fix lighting uniform alignment mismatch (GPU memory corruption)
    File: src/systems/render/lighting.rs:7-13
    Fix: Change [f32; 3] to [f32; 4] in LightUniform and FogUniform
         to match WGSL's 16-byte alignment requirement for vec3 in
         uniform buffers. Update shader to use .xyz.
    Effort: Small

[ ] P3. Fix stuck mouse input (fire_primary/fire_secondary never reset)
    File: src/systems/input/manager.rs:104-108
    Fix: Call reset_combat_inputs() at end of each frame in app.rs
         or update_visuals(), OR change to edge-triggered pattern
         (take_fire_primary() that reads + clears).
    Effort: Small

[ ] P4. Fix glTF normals not loaded (broken lighting on .glb/.gltf)
    File: src/systems/render/mesh.rs:70-130
    Fix: Add reader.read_normals() in load_gltf() similar to how
         UVs are read. Currently all glTF vertices get [0,1,0]
         hardcoded normals. OBJ loader works correctly.
    Effort: Small

================================================================================
PRIORITY 2 🟡 MUST FIX — Design flaws that cause incorrect behavior
================================================================================

[ ] P5. Fix framerate-dependent cooldown timer
    File: src/core/engine/update.rs:32-34, 53-56
    Fix: Change action_cooldown from frame-counter to time-based
         f32 decremented by dt. Fixes debug logging rate and
         combat cooldown varying with FPS.
    Effort: Small

[ ] P6. Fix camera spawn not matching level data
    File: src/core/engine/state.rs:246 vs src/data/world/level.rs:102
    Fix: Initialize camera.position from level_data.player_spawn
         + Y offset (124.5) instead of hardcoding (-10, 156, -10).
         Ensures player starts where level designer intended.
    Effort: Small

[ ] P7. Fix duplicate key binding: "dash" and "sprint" both = SHIFT
    File: src/data/config/gameplay.rs:78-79
    Fix: Choose a different default for one action (e.g., sprint=Shift,
         dash=Q or double-tap Shift). Update config/bindings.toml.
    Effort: Small

[ ] P8. Add error message for silently swallowed prop load failures
    File: src/core/engine/state.rs:321-326
    Fix: Add eprintln! in the else branch of catch_unwind when
         a prop model fails to load. Currently failures are silent.
    Effort: Trivial

[ ] P9. Fix Renderer being a pointless thin facade
    File: src/systems/render/renderer.rs (38 lines)
    Fix: Either inline into app.rs, or give Renderer actual
         responsibilities (state management, frame pacing, etc.).
    Effort: Medium

================================================================================
PRIORITY 3 🟡 SHOULD FIX — Design cleanup and dead code removal
================================================================================

[ ] P10. Remove or integrate dead src/data/world/spawn.rs
    File: src/data/world/spawn.rs (30 lines)
    Status: Entire file #[allow(dead_code)] suppressed. SpawnPoint
            not used anywhere (spawn comes from LevelData directly).
    Fix: Remove file + module decl, OR integrate into level system.

[ ] P11. Remove or integrate dead src/data/world/strata.rs
    File: src/data/world/strata.rs (82 lines)
    Status: #[allow(dead_code)] suppressed. StratumId enum (7 biomes)
            and Stratum struct not referenced anywhere.
    Fix: Remove, OR wire into engine so player Y determines active
         stratum, enabling biome-specific fog/lighting.

[ ] P12. Remove or integrate dead src/game/ module (~444 lines)
    Files: src/game/gameplay/stamina.rs  (70 lines)
           src/game/gameplay/player.rs   (92 lines)
           src/game/gameplay/movement.rs (170 lines)
           src/game/gameplay/mod.rs      (12 lines)
           src/game/mod.rs               (1 line)
    Status: Entire module #[allow(dead_code)] suppressed. Duplicates
            live movement/stamina systems in camera.rs + physics.
    Fix: Either (a) remove entirely, (b) merge sprint/dash/stamina
         into live engine and remove dead copies, or (c) preserve
         as reference with documentation.

[ ] P13. Make level name configurable instead of hardcoded
    File: src/app.rs:44
    Fix: Accept level name from CLI args (std::env::args()) or
         config file. Fall back to "ashwalk_01".
    Effort: Small

[ ] P14. Enable runtime key remapping (remove serde skip on keys)
    File: src/data/config/gameplay.rs:15
    Fix: Remove #[serde(skip_serializing, skip_deserializing)] on
         the keys HashMap. Split into separate file if needed.
    Effort: Small

================================================================================
PRIORITY 4 🟢 NICE TO HAVE — Code quality and minor cleanup
================================================================================

[ ] P15. Move import to top of file
    File: src/core/engine/loader.rs:263 (use wgpu::util::DeviceExt)
    Fix: Move to top of file with other imports.

[ ] P16. Remove stale comments about removed modules
    File: src/data/world/mod.rs:9-10
    Fix: Clean up references to removed bells/shortcuts/hazards.

[ ] P17. Fix misleading GROUND_RAY_LENGTH documentation
    File: src/systems/physics/engine.rs:14
    Fix: Update comment to explain 0.7 = 0.6 (radius) + 0.1 margin.

[ ] P18. Document or gitignore cenotaph2600.bin
    Location: Project root
    Fix: Either document purpose or add to .gitignore.

[ ] P19. Create starter textures/ directory
    Problem: No textures exist; all models show fallback checkerboard
    Fix: Add a simple default texture or document requirement.

================================================================================
PRIORITY 5 🔵 FUTURE — Feature work (stubs waiting for implementation)
================================================================================

[ ] P20. Implement combat/damage system
    File: src/core/engine/update.rs:86-116
    Current: LMB raycast detects hits but only prints debug message
    Needs: Wire fire_primary → damage → enemy health → prop removal

[ ] P21. Wire combat input flags to actual game logic
    File: src/systems/input/manager.rs:17-24
    Current: fire_primary, fire_secondary, reload, dash, aim,
             weapon_swap, selected_art all set but never read
    Needs: Consumer code that reads these flags

[ ] P22. Implement sprint/dash with stamina consumption
    Files: Dead: movement.rs, stamina.rs (have full implementation)
           Live: camera.rs + physics/engine.rs (basic movement only)
    Needs: Merge dead module's sprint/dash/stamina into live engine

[ ] P23. Wire PropData gameplay fields into engine
    Fields: item_id, enemy_type, enemy_health, light_color,
            ambient_sound_id, trigger_level_id
    Current: All defined with types/docs but never consumed
    Needs: Respective systems (items, enemies, per-prop lights,
            sounds, level transitions)

================================================================================
QUICK REFERENCE — File index of all issues
================================================================================

File                                   Issues
────────────────────────────────────── ────────────────────────────────────
src/app.rs                             P13 (hardcoded level name)
src/core/engine/state.rs               P6 (camera spawn), P8 (silent errors)
src/core/engine/update.rs              P5 (frame cooldown), P20 (combat stub)
src/core/engine/loader.rs              P15 (import placement)
src/data/config/gameplay.rs            P7 (duplicate keys), P14 (serde skip)
src/data/world/mod.rs                  P16 (stale comments)
src/data/world/spawn.rs                P10 (dead file)
src/data/world/strata.rs               P11 (dead file)
src/game/ (entire module)              P12 (dead module)
src/systems/input/manager.rs           P3 (stuck input), P21 (unused flags)
src/systems/physics/engine.rs          P1 (compile error), P17 (doc)
src/systems/render/lighting.rs         P2 (alignment)
src/systems/render/mesh.rs             P4 (normals)
src/systems/render/renderer.rs         P9 (facade)
Project root                           P18 (cenotaph2600.bin)
textures/                              P19 (missing dir)